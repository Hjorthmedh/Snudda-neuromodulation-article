The data to generate this figure is in the folder ```virtual_experiments/network_example```. On Dardel you can run ```Dardel_simulate_network_dspn_only.job``` to create the network stored in ```networks/striatum_with_dopamine_dspn_only``` subfolder.

The run ```InspectNetworkSimulation.ipynb``` to generate the figures.

